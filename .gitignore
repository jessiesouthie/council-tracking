.venv/
__pycache__/
*.pyc
output/*.xlsx
# Agendas are a scratch download for ingest.build_upcoming and
# ingest.build_agenda_only — cheap to re-fetch, and superseded by the minutes in
# data/raw for any meeting that gets minuted. Only the JSON they write ships.
data/agendas/
.DS_Store

# Voiceprint work: the speaker-matching venv and its cached model/samples are
# all rebuildable from data/voiceprints/*.json — see ingest/voiceprints.py.
.venv-voice/
data/voiceprints/.model/
data/voiceprints/cache/
