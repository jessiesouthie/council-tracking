.venv/
__pycache__/
*.pyc
output/*.xlsx
# Agendas are a scratch download for ingest.build_upcoming — superseded by the
# minutes in data/raw once the meeting happens. Only the JSON it writes ships.
data/agendas/
.DS_Store
